<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">
    <Dialog
        id="dialogNewProduct"
        title="Create New Product"
        contentWidth="30rem">
        <f:SimpleForm
            editable="true"
            layout="ResponsiveGridLayout">
            <f:content>
                <Label text="Product ID" />
                <Input value="{NewProduct>/ProductName}" />

                <Label text="Product Name" />
                <Input value="{NewProduct>/ProductName}" />

                <Label text="Unit Price" />
                <Input value="{NewProduct>/UnitPrice}" type="Number" />

                <Label text="Units In Stock" />
                <Input value="{NewProduct>/UnitsInStock}" type="Number" />

                <Label text="Discontinued" />
                <CheckBox selected="{NewProduct>/Discontinued}" />

                <Select
                    forceSelection="false"
                    selectedKey="{/SelectedProduct}"
                    items="{
						path: 'Suppliers>/',
						sorter: { path: 'Name' }
					}">
                    <core:Item key="{Suppliers>SupplierID}" text="{Suppliers>SupplierID}" />
                </Select>
            </f:content>
        </f:SimpleForm>
        <buttons>
            <Button
                text="Save"
                type="Emphasized"
                press="onSaveNewProduct" />
            <Button
                text="Cancel"
                press="onCancelNewProduct" />
        </buttons>
    </Dialog>
</core:FragmentDefinition>